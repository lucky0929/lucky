<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="dao.IMemberDao">
  
  <insert id="insertMember" parameterType="java.util.HashMap">
  insert into member(name, userid, pwd, email) values(#{name}, #{userid}, #{pwd}, #{email})
  </insert>
  
  <select id="selectOne" parameterType="String" resultType="java.util.HashMap">
  select * from member where userid = #{userid}
  </select>
  
  <select id="login" parameterType="java.util.HashMap" resultType="int">
  select COUNT(*) from member where userid =#{userid} and pwd = #{pwd}
  </select>
  
  
  
  </mapper>